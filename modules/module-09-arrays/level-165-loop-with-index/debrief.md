# Debrief

You practiced `Loop with Index` with the Bash array pattern ``for i in "${!arr[@]}"``. This is a building block for scripts that keep lists, lookup tables, and structured command output in memory.
