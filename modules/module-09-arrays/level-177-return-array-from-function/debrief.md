# Debrief

You practiced `Return Array from Function` with the Bash array pattern `output parsing`. This is a building block for scripts that keep lists, lookup tables, and structured command output in memory.
