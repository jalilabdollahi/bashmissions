# Debrief

You practiced `Array from Command` with the Bash array pattern ``mapfile -t arr < <(cmd)``. This is a building block for scripts that keep lists, lookup tables, and structured command output in memory.
