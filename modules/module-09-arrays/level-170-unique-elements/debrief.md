# Debrief

You practiced `Unique Elements` with the Bash array pattern `sort + uniq trick`. This is a building block for scripts that keep lists, lookup tables, and structured command output in memory.
