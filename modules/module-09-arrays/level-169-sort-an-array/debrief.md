# Debrief

You practiced `Sort an Array` with the Bash array pattern ``readarray` + `sort``. This is a building block for scripts that keep lists, lookup tables, and structured command output in memory.
