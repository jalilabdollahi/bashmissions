# Debrief

You practiced `Pass Array to Function` with the Bash array pattern `nameref `declare -n``. This is a building block for scripts that keep lists, lookup tables, and structured command output in memory.
