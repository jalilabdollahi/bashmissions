# Debrief

You practiced `Check Key Exists` with the Bash array pattern ``[[ -v map[key] ]]``. This is a building block for scripts that keep lists, lookup tables, and structured command output in memory.
