# Solution Guide: JSON POST

This level focuses on `curl -H "Content-Type: application/json"`.

```bash
#!/usr/bin/env bash
set -euo pipefail
header='Content-Type: application/json'
json='{"name":"ada"}'
echo "header=$header"
echo "json=$json"
```

The script demonstrates the concept safely inside the mission workspace.
