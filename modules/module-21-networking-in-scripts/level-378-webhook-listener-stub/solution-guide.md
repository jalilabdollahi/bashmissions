# Solution Guide: Webhook Listener Stub

This level focuses on `nc` listener.

```bash
#!/usr/bin/env bash
set -euo pipefail
port=8080
echo "listener=nc -l $port"
```

The script demonstrates the concept safely inside the mission workspace.
