id: 141
module: 7
module_name: string-manipulation
module_display: String Manipulation
title: Regex Match
difficulty: intermediate
xp: 135
objective: 'Write `solution.sh` so it prints exactly `LEVEL 141: Regex Match | <arg1>
  | <arg2>`.

  Use the script arguments as `<arg1>` and `<arg2>`, preserving spaces exactly.

  Exit with status 0 on success.'
expected_script_name: solution.sh
test_cases:
- args:
  - alpha
  - beta
  expected_stdout: 'LEVEL 141: Regex Match | alpha | beta'
  expected_exit: 0
  comparison: exact
- args:
  - spaces allowed
  - '42'
  expected_stdout: 'LEVEL 141: Regex Match | spaces allowed | 42'
  expected_exit: 0
  comparison: exact
fixtures:
- data.txt
concepts:
- '`[[ $str =~ regex ]]`'
scaffold: true
