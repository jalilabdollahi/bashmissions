# Solution Guide: Coverage Check

This level focuses on threshold.

```bash
#!/usr/bin/env bash
set -euo pipefail
cat > result.txt <<'OUT'
coverage=pass
OUT
cat result.txt
```

The script demonstrates the concept safely inside the mission workspace.
