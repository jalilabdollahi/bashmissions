id: 86
module: 5
module_name: loops
module_display: Loops
title: Loop over Arguments
difficulty: beginner
xp: 60
objective: Loop over all command-line arguments with `for arg in "$@"` and print each
  as `arg=<value>`.
expected_script_name: solution.sh
test_cases:
- args:
  - alpha
  - beta
  expected_stdout: 'arg=alpha

    arg=beta'
  expected_exit: 0
  comparison: exact
- args:
  - spaces allowed
  - '42'
  expected_stdout: 'arg=spaces allowed

    arg=42'
  expected_exit: 0
  comparison: exact
fixtures:
- data.txt
concepts:
- '`for arg in "$@"`'
scaffold: true
