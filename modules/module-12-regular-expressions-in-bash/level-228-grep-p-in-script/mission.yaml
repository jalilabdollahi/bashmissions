id: 228
module: 12
module_name: regular-expressions-in-bash
module_display: Regular Expressions in Bash
title: grep -P in Script
difficulty: intermediate
xp: 155
objective: 'Write `solution.sh` so it prints exactly `LEVEL 228: grep -P in Script
  | <arg1> | <arg2>`.

  Use the script arguments as `<arg1>` and `<arg2>`, preserving spaces exactly.

  Exit with status 0 on success.'
expected_script_name: solution.sh
test_cases:
- args:
  - alpha
  - beta
  expected_stdout: 'LEVEL 228: grep -P in Script | alpha | beta'
  expected_exit: 0
  comparison: exact
- args:
  - spaces allowed
  - '42'
  expected_stdout: 'LEVEL 228: grep -P in Script | spaces allowed | 42'
  expected_exit: 0
  comparison: exact
fixtures:
- data.txt
concepts:
- Perl regex (where available)
scaffold: true
