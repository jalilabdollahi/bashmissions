# Solution Guide: Sort File

This level focuses on `sort` in script.

```bash
#!/usr/bin/env bash
set -euo pipefail

sort fixtures/data.txt
```

The script performs the file or text operation directly, then prints deterministic output for the checker.
