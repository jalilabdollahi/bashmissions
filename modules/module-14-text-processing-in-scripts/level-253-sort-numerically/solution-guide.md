# Solution Guide: Sort Numerically

This level focuses on `sort -n`.

```bash
#!/usr/bin/env bash
set -euo pipefail

sort -n fixtures/data.txt
```

The script performs the file or text operation directly, then prints deterministic output for the checker.
