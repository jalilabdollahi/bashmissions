id: 46
module: 3
module_name: arguments-input
module_display: Arguments & Input
title: Read Silently
difficulty: beginner
xp: 60
objective: 'Use `read -r -s` to read a password from the file path in `$1`, then print
  only `Password length: N`, where `N` is the number of characters. Do not print the
  password.'
expected_script_name: solution.sh
test_cases:
- args:
  - fixtures/data.txt
  expected_stdout: 'Password length: 9'
  expected_exit: 0
  comparison: exact
fixtures:
- data.txt
concepts:
- '`read -s` for passwords'
scaffold: true
