id: 53
module: 3
module_name: arguments-input
module_display: Arguments & Input
title: Handle Quoted Arguments
difficulty: beginner
xp: 80
objective: Loop over `"$@"` and print each argument on its own numbered line as `argN=<value>`.
  Quoted arguments containing spaces must remain intact.
expected_script_name: solution.sh
test_cases:
- args:
  - alpha
  - two words
  - gamma
  expected_stdout: 'arg1=alpha

    arg2=two words

    arg3=gamma'
  expected_exit: 0
  comparison: exact
- args:
  - one big argument
  expected_stdout: arg1=one big argument
  expected_exit: 0
  comparison: exact
fixtures:
- data.txt
concepts:
- quoting in `"$@"`
scaffold: true
