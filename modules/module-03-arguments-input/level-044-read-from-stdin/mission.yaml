id: 44
module: 3
module_name: arguments-input
module_display: Arguments & Input
title: Read from Stdin
difficulty: beginner
xp: 90
objective: 'Read one line from the file path in `$1` using `read -r value < "$1"`,
  then print `You said: <value>`. This practices the same `read` pattern used for
  stdin while keeping tests non-interactive.'
expected_script_name: solution.sh
test_cases:
- args:
  - fixtures/data.txt
  expected_stdout: 'You said: hello there'
  expected_exit: 0
  comparison: exact
fixtures:
- data.txt
concepts:
- '`read var`'
scaffold: true
