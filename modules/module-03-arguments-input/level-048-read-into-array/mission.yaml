id: 48
module: 3
module_name: arguments-input
module_display: Arguments & Input
title: Read into Array
difficulty: beginner
xp: 80
objective: Use `read -r -a words` to split the first line of the file path in `$1`
  into an array. Print `count=<n> first=<first> last=<last>`.
expected_script_name: solution.sh
test_cases:
- args:
  - fixtures/data.txt
  expected_stdout: count=3 first=alpha last=gamma
  expected_exit: 0
  comparison: exact
fixtures:
- data.txt
concepts:
- '`read -a arr`'
scaffold: true
